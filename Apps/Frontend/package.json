{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:main && npm run build:styles && node tools/build.js",
    "build:main": "esbuild src/main.js --outdir=dist --bundle --minify",
    "build:legacy": "spack && swc dist/legacy.js -d dist/",
    "build:styles": "esbuild src/main.css --outdir=dist --minify",
    "dev": "swc --watch src/main.js -d dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.44",
    "core-js": "^3.29.1",
    "esbuild": "^0.17.14",
    "esbuild-plugin-es5": "^1.2.0"
  },
  "msw": {
    "workerDirectory": "public"
  },
  "dependencies": {
    "ejs": "^3.1.9",
    "fetch-polyfill": "^0.8.2",
    "promise-polyfill": "^8.3.0",
    "yaml": "^2.2.1"
  }
}
