{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run bundle && npm run build:main && npm run build:legacy",
    "build:main": "swc dist/main.js -d dist/",
    "build:legacy": "swc dist/ie.js --config-file .swcrc-legacy -d dist/",
    "bundle": "npx spack",
    "dev": "swc --watch src/main.js -d dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.41",
    "core-js": "^3.29.1"
  },
  "msw": {
    "workerDirectory": "public"
  },
  "dependencies": {
    "fetch-polyfill": "^0.8.2",
    "promise-polyfill": "^8.3.0"
  }
}
